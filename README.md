# dun_diary_app

A new Flutter project.

## Getting Started
This project uses **FVM** to manage Flutter version.

1. Install FVM: `dart pub global activate fvm`
2. Install dependencies: `fvm install`
3. Run App: `fvm flutter run`
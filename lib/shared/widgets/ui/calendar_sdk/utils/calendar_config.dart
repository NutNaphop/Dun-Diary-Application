// Config Caldendar
class CalendarConfig {
  static String locale = "th_TH";
  static DateTime now = DateTime.now();
  static DateTime firstDay = DateTime(now.year - 100, 1, 1);
  static DateTime lastDay = DateTime(now.year + 10, 12, 31);
}
